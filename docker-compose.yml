services:
  license-monitor:
    build: .
    container_name: license-monitor
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
